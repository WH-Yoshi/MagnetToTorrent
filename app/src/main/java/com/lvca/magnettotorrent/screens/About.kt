package com.lvca.magnettotorrent.screens

import androidx.compose.runtime.Composable
import androidx.navigation.NavController
import com.lvca.magnettotorrent.MainViewModel

@Composable
fun AboutScreen(
    navController: NavController,
    viewModel: MainViewModel
) {
    // This screen is intentionally left empty as per the original code.
    // You can implement the UI for the About screen here if needed.
}